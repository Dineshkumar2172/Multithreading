public class ProcessAndThreads {
    /*
     * 1. Process is an instance of program execution. When you enter an application, it's a process. The OS assigns it's own stack & heap memory area.
     * 
     * 2. Thread is a lightweight process. It is a unit of execution within a given program. A single process may contains multiple threads. Each thread in the memory shares the memory and resources.
     */
}
